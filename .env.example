# Copy to .env and fill in only if you want to run the real judge models.
# This repo's tests run 100% offline with the stubbed judge and need NO keys.
OPENAI_API_KEY=
# deepeval opt-out of anonymous usage telemetry (also set in CI):
# https://docs.confident-ai.com/docs/evaluation-introduction
DEEPEVAL_TELEMETRY_OPT_OUT=1
